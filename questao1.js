let indice = 13;
let soma = 0;

    for (k = 0; k < indice; k++) {
        soma = soma + k;
        if (k >= indice){
        break;
        }
    }
    
console.log(`O valor da soma é ${soma}`);
